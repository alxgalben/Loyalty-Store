{% extends 'backend-layout.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block content %}
    <div class="content-wrapper">
        <div class="content-header">
            <div class="container-fluid">
                <div class="row mb-2">
                    <div class="col-sm-6">
                        <h1 class="m-0">Dashboard</h1>
                    </div><!-- /.col -->
                    <div class="col-sm-6">
                        <ol class="breadcrumb float-sm-right">
                            <li class="breadcrumb-item"><a href="#">Home</a></li>
                            <li class="breadcrumb-item active">Dashboard</li>
                        </ol>
                    </div><!-- /.col -->
                </div><!-- /.row -->
            </div><!-- /.container-fluid -->
        </div>
        <div class="card">
            <div class="card-header border-0">
                <h3 class="card-title">Accounts management</h3>
                <div class="card-tools">
                    <a href="#" class="btn btn-tool btn-sm">
                        <i class="fas fa-download"></i>
                    </a>
                    <a href="#" class="btn btn-tool btn-sm">
                        <i class="fas fa-bars"></i>
                    </a>
                </div>
            </div>
            <div class="card-body table-responsive p-0">
                <table class="table table-striped table-valign-middle">
                    <thead>
                    <tr>
                        <th>ID</th>
                        <th>Email</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for account in accounts %}
                        <tr>
                            <td><b>{{ account.id }}</b></td>
                            <td><b>{{ account.email }}</b></td>
                            <td>
                                <div class="d-flex align-items-center">

                                    <a href="{{ path('set-user-actions', {'user': 'accounts', 'id': account.id, 'action': 'edit'}) }}"
                                       class="btn btn-primary mr-2">Edit</a>
                                    <a href="{{ path('set-user-actions', {'user': 'accounts', 'id': account.id, 'action': 'delete'}) }}"
                                       class="btn btn-danger" style="margin-right: 10px;">Delete</a>
                                    {% if not account.isActive %}
                                        <a href="{{ path('set-user-actions', {'user': 'accounts', 'id': account.id, 'action': 'activate'}) }}">
                                            <button type="button" class="btn btn-success">mark as active</button>
                                        </a>
                                    {% else %}
                                        <a href="{{ path('set-user-actions', {'user': 'accounts', 'id': account.id, 'action': 'deactivate'}) }}">
                                            <button type="button" class="btn btn-secondary">mark as inactive</button>
                                        </a>
                                    {% endif %}

                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
